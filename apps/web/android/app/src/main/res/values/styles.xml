<?xml version="1.0" encoding="utf-8" ?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style
    name="AppTheme.NoActionBar"
    parent="Theme.AppCompat.DayNight.NoActionBar"
  >
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
    <item name="android:background">@drawable/splash</item>
</style>

<!-- Base Kpocha style (required because something references @style/Kpocha) -->
<style name="Kpocha" />

<!-- Native feed / reels small counter text -->
<style name="Kpocha.CounterText">
    <item name="android:textColor">@color/kpocha_text</item>
    <item name="android:textSize">@dimen/counter_text_size</item>
</style>

<style name="Kpocha.CounterText.Muted">
    <item name="android:textColor">@color/kpocha_muted</item>
    <item name="android:textSize">@dimen/counter_text_size</item>
</style>

</resources>
